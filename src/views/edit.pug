extends base.pug

block content
  h4 Change Title of video
  form(action="", method="POST")
    input(
      name="title",
      placeholder=`${pageTitle}`,
      value=`${video.title}`,
      required,
      type="text"
    )
    input(value="Save", type="submit")
